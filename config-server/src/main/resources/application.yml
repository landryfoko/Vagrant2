server:
  port: 7000

management:
  context-path: /admin
  
logging:
  level:
    com.netflix.discovery: 'OFF'
    org.springframework.cloud: 'DEBUG'
  

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LibertasNet/vipass-config
          basedir: /var/libertas/git/vipass-config
          password: man2than4..
          username: fenkam
  